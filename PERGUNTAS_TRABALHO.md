# Perguntas

As seguintes perguntas serão feitas por sorteio na data de apresentação do trabalho. Você receberá até 2 perguntas
para serem respondidas de maneira oral. 

## HTML

1. Por que usamos `<!DOCTYPE html>` no topo do documento HTML?
2. para gente começar a pagina
2. Qual a diferença entre `<head>` e `<body>`?
3. hea coloca o style, body usa h1,table entre outros corpo da pagina
3. Para que serve a tag `<title>`? Onde ela fica localizada no documento HTML, e por quê?
4.tela titulo do meu site
5. O que é um charset? Cite dois exemplos de charset.
6.  charset coloco o idioma, ate outros charset outras iso, latinos.
5. Qual a diferença entre `<div>` e containers semânticos? Cite um exemplo.
6. div organizar a pagina em blocos
7. Qual a diferença entre `<ul>` e `<ol>`?
8. ol ordena lista numerada,ul lista bolinhas, nao numerada
7. Qual a função da tag `<a>`? 
8. tag link
8. Para que serve o atributo `alt` na `<img>`?
9. descricao da imagem
9. Qual a importância das tags `h1` até `h6`?
10. tamanho paragrafo
10. Quantas tags `<h1>` uma página deve ter?
11. precisa ter uma so

## CSS

1. O que é CSS? Para que é usado?
2. css usado colocar style
2. Quais são as três formas de utilizar CSS em um documento Web?
3. Cite os 4 seletores CSS mais comuns e dê um exemplo de cada um no seu código.
4. O que é um seletor composto? Dê um exemplo de seletor composto.
5. O que acontece se dois estilos entram em conflito?
6. Qual a diferença entre `margin` e `padding`?
7. margin aparte externo, paddin a parte interno
7. Para que serve o `:hover`?  
8. colocar cor quando usuario por emcima boton
8. O que são media queries? Para que servem? Dê um exemplo no seu código.
9. Qual a utilidade da responsividade em um site?
10. Qual a diferença entre , vw, px, % e em?
px porcentagem,vr largura tela usuario,,vr altura tela usuario,em tamanho da fonte.


## Javascript

1. A linguagem Javascript é compilada ou interpretada? Por quê?
2. Para quê serve Javascript? É possível escrever um site sem Javascript? Por quê?
3. Onde o código Javascript é executado: no servidor ou no navegador?
4. Como selecionar um elemento no Document Object Model?
5. Mostre ou implemente uma função no seu código-fonte.
6. Qual a diferença entre `getElementById` e `querySelector`?
7. Cite **dois** exemplos de eventos em Javascript. Implemente
   ou mostre a implementação de **um** evento no seu código-fonte.
8. Qual a diferença entre `let` e `var`? Cite um exemplo.
9. O que é um array? Para que é utilizado? Mostre no seu código ou implemente
   um array.
10. Qual a diferença entre `NaN`, `null` e `undefined`? Dê um exemplo de cada um no seu código-fonte.
